# Dynamic Risk Assessment Projcet

We use `uv` for packet management.
To get started please setup the envirment using `uv sync`

## Data Ingestion 
The data ingestion can be run using:

```bash
uv run ingestion.py
```


uvx ruff check . --fix && uvx ruff format
